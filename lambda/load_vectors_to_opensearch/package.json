{
  "name": "load-vectors-to-opensearch",
  "version": "1.0.0",
  "description": "Lambda function to load vector embeddings into AWS OpenSearch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@opensearch-project/opensearch": "^1.1.0",
    "aws-sdk": "^2.1330.0"
  }
}